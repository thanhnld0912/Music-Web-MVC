@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model string

<div class="sidebar" id="sidebar">
    <button class="sidebar-toggle" onclick="toggleSidebar(event)">☰</button>
    <div class="logoimg">
        <a href="@Url.Action("NewFeedAdmin", "Home")">
            <img src="~/img/lgo.png" alt="Logo"/>
        </a>
    </div>
    <div class="logo">Melofy</div>
    <button class="sidebar-button @(Model == "dashboard" ? "active" : "")"
            onclick="window.location.href='@Url.Action("Dashboard", "Admin")'">
        Dashboard
    </button>

    <button class="sidebar-button @(Model == "user" ? "active" : "")"
            onclick="window.location.href='@Url.Action("User", "Admin")'">
        User
    </button>

    <button class="sidebar-button @(Model == "post" ? "active" : "")"
            onclick="window.location.href='@Url.Action("Post", "Admin")'">
        Post
    </button>

</div>
